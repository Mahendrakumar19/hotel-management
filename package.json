{
  "name": "hotel-management-system",
  "version": "1.0.0",
  "description": "Complete Hotel Management System with Room Reservation, Billing, and Store Management",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "postinstall": "npm install --prefix backend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "start": "npm start --prefix backend",
    "test": "npm test --prefix backend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend"
  },
  "keywords": [
    "hotel",
    "management",
    "reservation",
    "billing",
    "nodejs",
    "react"
  ],
  "author": "Hotel Management Team",
  "license": "MIT",
  "dependencies": {}
}
